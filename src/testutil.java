import java.util.Arrays;

public class testutil extends utils {
    public static void main(String[] args) {
        int[] numbers = manualInput();
        printlnstr(Arrays.toString(numbers));
        }
    }
